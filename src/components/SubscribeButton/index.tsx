import styles from './styles.module.scss'

export const SibscribeButton = () => {
    return (
        <button
            type="button"
            className={styles.sibscribeButton}
        >
            Subscribe now
        </button>
    )
}